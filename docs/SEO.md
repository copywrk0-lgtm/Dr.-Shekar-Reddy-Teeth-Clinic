# SEO launch notes

The project includes route-aware title/description/canonical updates, Dentist JSON-LD, robots, sitemap, Open Graph metadata, an OG image, and descriptive treatment URLs.

Before launch:
- replace the placeholder production domain
- connect Google Search Console
- submit the sitemap
- verify the Google Business Profile URL
- verify clinic NAP details
- add the real social profile URLs to `sameAs` if available
- confirm all structured-data claims
